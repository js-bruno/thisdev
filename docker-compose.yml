wervices:
    web:
      image: nginx:alpine
      container_name: astro_ngnix
      ports: 
        - "8080:80"
      volumes:
        - ./dist:/usr/share/nginx/html:ro
        - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      restart: unless-stopped
